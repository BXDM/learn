! 计算圆的半径和周长

program main
!real r ,l,s,     ! 定义三个实型常量
!real pi = 3.14159
!read *，r;     !读入半径
!l = 2*pi*r     !计算周长
!s = pi*r**     !计算面积
!print *,r,p,s  !显示计算结果
write(*.*) hello,world
end program sample         !程序计算完毕